[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# We removed the [[headers]] section entirely to stop the blank page.
# Once your credits refresh, this will allow the site to render normally.
